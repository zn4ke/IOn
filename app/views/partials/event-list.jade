div( ng-controller="ViewerCtrl")
    div.container
        h3 Veranstaltungen
        h4 meine:
        div.col-xs-12
            .row
                div( ng-repeat="event in data.events.pub", ng-click="joinEvent(this)")
                    .thumbnail
                        div name {{event.name}}
                        div date {{event.date}}
                        div pub {{event.pub}}
        h4 öffentliche
        div.col-xs-12
            .row
                div( ng-repeat="event in data.events.my", ng-click="joinEvent(this)" )
                    .thumbnail
                        div name {{event.name}}
                        div date {{event.date}}
                        div pub {{event.pub}}
        br
    